{
  "name": "@midnight-devkit/monorepo",
  "version": "1.0.0",
  "description": "A comprehensive developer toolkit for building on Midnight Network",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "keywords": [
    "midnight",
    "blockchain",
    "zero-knowledge",
    "developer-tools",
    "cli",
    "zk-proofs"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
